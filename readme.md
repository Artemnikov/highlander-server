This project is a node.js express app that uses typescripy.

To run the app.
Make sure you are running node.js 16.18.0 using nvm.
Then run the command npm i
And for developement server run npm run dev.